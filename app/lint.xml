<?xml version="1.0" encoding="UTF-8"?>
<!-- قرارات lint الموثقة لمشروع قلعة بوكس -->
<lint>
    <!-- قرار معماري: المحرك (Wine + Box86/64) يستهدف arm64-v8a حصراً —
         دعم ChromeOS x86_64 غير مطلوب في هذه المرحلة -->
    <issue id="ChromeOsAbiSupport" severity="ignore" />

    <!-- بنية اعتماديات مثبتة ومتوافقة مع compileSdk 34 — الترقية الجماعية
         لكثيرات AndroidX الأحدث تتطلب رفع compileSdk وتحتاج اختباراً مستقلاً -->
    <issue id="GradleDependency" severity="ignore" />

    <!-- targetSdk 28 مقصود وجوهري (راجع FIXES.md #35): أندرويد 10+ يمنع تنفيذ
         ملفات مجلد بيانات التطبيق (W^X) للتطبيقات ذات targetSdk ≥ 29 — وtargetSdk 28
         يبقي التطبيق في نطاق SELinux القديم الذي يسمح بتنفيذ ثنائيات المحاكي -->
    <issue id="OldTargetApi" severity="ignore" />
    <issue id="ExpiredTargetSdkVersion" severity="ignore" />
</lint>
